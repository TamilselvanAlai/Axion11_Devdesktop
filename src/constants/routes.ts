export const ROUTES = {
  login: "/login",
  dashboard: "/dashboard",
  projects: "/projects",
  recent: "/recent",
  transfers: "/transfers",
  reports: "/reports",
  settings: "/settings",
} as const;
